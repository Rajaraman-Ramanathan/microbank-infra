---
- name: Show PostgreSQL port
  ansible.builtin.debug:
    msg: "PostgreSQL Port = {{ postgres_port }}"

- name: Display configured databases
  ansible.builtin.debug:
    msg: "Database {{ item.database }} will be created for user {{ item.username }}"
  loop: "{{ postgres_databases }}"

#- name: Verify PostgreSQL connectivity
#   community.postgresql.postgresql_ping:
#   login_host: "{{ rds_endpoint }}"
#   login_user: "{{ master_username }}"
#   login_password: "{{ master_password }}"
#   login_db: "{{ postgres_database }}"
#   port: "{{ postgres_port }}"
#   ssl_mode: "{{ postgres_ssl_mode }}" 